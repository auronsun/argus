fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# Market data — free tier
yfinance>=0.2.50
akshare>=1.15

# Data / indicators (indicators implemented directly on pandas/numpy — no ta-lib)
pandas>=2.2
numpy>=1.26
pyarrow>=18.0

# HTTP / async
httpx>=0.27
websockets>=13.0

# Storage
sqlmodel>=0.0.22

# LLM SDKs (all optional at runtime — imported lazily)
anthropic>=0.40
openai>=1.55

# Utility
loguru>=0.7
tenacity>=9.0
